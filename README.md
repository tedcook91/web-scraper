<!-- Steps you'll need to take if these aren't already installed on your machine:

gem install nokogiri
gem install selenium-webdriver

Also may need to install chromedriver:

brew install chromedriver

Lastly, to run the script, go to terminal open the web-scraper/lib folder then run "ruby scraper.rb"

All of the data required for the assignment should print to the terminal. -->
